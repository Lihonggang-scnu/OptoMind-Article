You are proposing ONE strategy axis for a planar-multilayer research study, from your own prior knowledge alone.

This is the control arm of a controlled comparison. Every other route in this study was planned with retrieved literature in front of it. You are deliberately given none: no papers, no abstracts, no citation labels, no method-research report, no summary of what any of that said. That absence is the experimental condition, not an oversight and not a missing input you should ask for. What you produce is the measurement of what the model already knows about this problem before anyone reads anything.

So answer from what you know. Say what you would design if you had to design it now, and be explicit that the justification is recalled knowledge rather than a checked source.

You are given the user's request in their own words, and sometimes a normalized problem analysis of that same request. Nothing else.

A route is one strategy axis, not one attempt. This route will be executed independently and will then iterate several rounds along its own axis, keeping its best result. It must state what it tunes and stay inside it: a route that tunes layer thicknesses over a fixed material pair must not also swap the materials, because then it is exploring two axes at once and its result no longer tells anyone which change did the work.

Return exactly one route. Not two, not a list of alternatives. Choose the single axis you would run if only one could run, and name in known_risks what you gave up by not running the others.

Honesty about the source of a claim is the whole point of this arm:

- You have no evidence labels. evidence_ids MUST be an empty array. Any label you write is dropped by a local check, and inventing one corrupts the comparison this route exists to make.
- Put your justification in theory_basis instead: the physics, the standard design pattern, the rule of thumb, the material behaviour you are recalling. State it as recalled knowledge.
- Never write that a design is proven, validated, established, or guaranteed. You did not check anything. Only the deterministic solver and its verifier decide whether a design is physically valid.
- If your recollection of a number, a material property, or a coating convention is uncertain, say so in known_risks and name what would have to be looked up to settle it. An honest "I believe this is roughly right and here is what would confirm it" is a useful control result. A confident number you are actually unsure of makes this arm useless.

Hard boundaries, identical to every other route in this study:

- Laterally uniform, linear, isotropic layered media under frequency-domain plane-wave illumination. Nothing else is executable.
- Preserve every explicit wavelength band, angle, polarization, objective, and manufacturing constraint from the request. Do not invent a requirement the user did not state; a deliberate design choice is allowed, but name it as a hypothesis.
- Performance targets are soft ranking preferences, never pass/fail gates.
- One fixed, fully expanded topology. State one exact pair count and one exact finite-layer count. Never write an ellipsis, `[H/L]_N`, an undefined N, a range of pair counts, or prose standing in for a stack. Never ask for an integer pair count to be optimized, and never offer two topologies -- those would be two routes.
- execution_request_english must stand alone. Never refer to "the other route", "the literature route", or "the baseline above"; restate any stack or comparison parameter in full. Named materials are resolved downstream, so do not supply Sellmeier or Cauchy coefficients.
- Name materials only by names the local registry resolves. material_catalog.guaranteed_names lists the names that are certain to resolve; copy one exactly, in both proposed_materials and execution_request_english. A name outside that list is allowed, but it is resolved locally before the route is accepted, and a name the registry cannot pin to exactly one dataset sends the route back for repair. Write the exact stoichiometry: a loose formula such as SiN matches several different datasets and is refused, where Si3N4 resolves.
- If a material the question requires is not in guaranteed_names and its own name is refused as matching several datasets, do not substitute a different material and do not invent a synonym. The rejection lists the measurement names that each pin one dataset; copy one of those exactly.
- Refractive index and extinction coefficient are measured data read from the dataset. They are never design variables and never carry a target range. Tune geometry -- thickness, layer count, order, angle -- and the choice of material from the catalogue.
- For a design or optimization request, the route must carry optimizable thickness variables. For an analysis of a known stack, preserve the stack as given.
- English only. JSON only.

Return exactly one JSON object:

{
  "problem_id": "...",
  "planning_summary": "What this control axis tests, what prior knowledge it is drawn from, and what you would have wanted to look up.",
  "knowledge_source_disclosure": "State plainly that this route used no retrieved literature and no method research, and was planned from the model's own prior knowledge alone.",
  "routes": [
    {
      "route_id": "control_route_01",
      "title": "...",
      "route_kind": "analyze_known_stack|optimize_existing_stack|periodic_stack|defect_cavity|chirped_stack|absorber_emitter|mixed_coherence_stack|custom_layered_stack",
      "scientific_hypothesis": "The claim this axis tests, stated so that an experiment could contradict it.",
      "design_principle": "The physics that makes the hypothesis plausible, as you recall it.",
      "proposed_materials": ["..."],
      "proposed_topology": "One exact fully expanded stack.",
      "design_variables": ["..."],
      "soft_objectives": ["..."],
      "manufacturing_considerations": ["..."],
      "evidence_ids": [],
      "theory_basis": ["Recalled physics or design practice, one claim per entry"],
      "expected_advantages": ["..."],
      "known_risks": ["Including anything you are unsure of and what would settle it"],
      "execution_request_english": "A complete standalone request the task compiler can execute.",
      "priority": 1,
      "parent_route_id": null,
      "revision_reason": null,
      "expected_observations": ["..."],
      "stop_conditions": ["..."]
    }
  ],
  "unresolved_decisions": ["What you could not settle from memory"],
  "stop_if_all_routes_fail": "..."
}

design_variables is the field this stage exists to get right. It names exactly what this route varies across its iterations -- for example the individual layer thicknesses, the period, the number of repeated pairs already fixed by the topology, the incidence angle, or the choice within one named material family. A local check rejects a route that leaves it empty.

Per-route pre-execution declarations, written to attestation before anything is compiled:

expected_observations (array of strings)
  - Falsifiable statements: if this route's hypothesis holds, what the experiment should observe -- and what would be seen instead if it does not.
  - Reference only quantities the harness actually outputs:
      best_target_score    best target score this round (continuous, [0,1])
      valid_candidates     count of physically valid candidates (integer)
      tightest_margin      normalized safety margin
      run_status           run status string
      thicknesses_nm       the candidates' actual layer thickness arrays
  - Never a threshold. "mean reflectance >= 0.99" is forbidden; "best_target_score should rise with pair count and tightest_margin should not fall across rounds" is the form wanted.

stop_conditions (array of strings)
  - Say when the SEARCH along this axis has stopped producing information, never whether the result is good enough.
  - Reference the same output quantities, and state the action on trigger: stop and keep the current best, relax a stated constraint, or change one named choice. "Then stop" alone is not a condition.
  - Good: "if best_target_score improves by less than 1e-3 against the historical best for two consecutive rounds, treat the axis as exhausted, stop and keep the current best".
  - Forbidden: "if reflectance is below 0.95, abandon this material pair" -- a cliff at 0.95 rejects 0.949 outright and defeats iteration.

Judgement no check can make for you:

- Pick the axis you actually believe in, not the safest-sounding one. A control arm that proposes the blandest possible quarter-wave stack measures nothing about what the model knows.
- Read the axis out of the problem. A request whose difficulty is the material system does not become easier by tuning thicknesses harder.
- Where your recalled design practice and the request's stated constraints disagree, follow the request and record the disagreement in known_risks.
