# Minimal runtime for the live research console.
#
# The public replay page itself does not need provider credentials or these
# packages. They are required only when a user starts a real research run.
# Keep the ranges aligned with the sibling VeriTMM engine and the harness
# contracts; the image intentionally does not install article-publication
# tooling or development-only packages.
numpy>=2.0,<3
scipy>=1.11,<2
pydantic>=2.10,<3
tmm>=0.2,<0.3
openai>=2.43,<3
ftfy>=6.3,<7
torch>=2.1,<3
